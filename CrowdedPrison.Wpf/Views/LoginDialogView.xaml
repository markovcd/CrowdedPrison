<UserControl
  x:Class="CrowdedPrison.Wpf.Views.LoginDialogView"
  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
  xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
  xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
  xmlns:viewModels="clr-namespace:CrowdedPrison.Wpf.ViewModels"
  Width="200"
  Height="100"
  d:DataContext="{d:DesignInstance viewModels:LoginDialogViewModel}"
  d:DesignHeight="450"
  d:DesignWidth="800"
  mc:Ignorable="d">
  <StackPanel>
    <TextBlock Text="{Binding WelcomeText}" TextAlignment="Center" />
    <TextBox Text="{Binding Email}" />
    <TextBox Text="{Binding Password}" />
    <Button Command="{Binding OkCommand}">OK</Button>
  </StackPanel>
</UserControl>
